spring:
  application:
    name: station
  profiles:
    active: test
  cloud:
    nacos:
      config:
        server-addr: ${nacos.hairoutech.com:172.18.35.23}:${nacos.port:8181}
        prefix: ${spring.application.name}
        file-extension: yml
        namespace: ${spring.profiles.active}
        group: swms
        username: nacos
        password: nacos

server:
  port: 9040
