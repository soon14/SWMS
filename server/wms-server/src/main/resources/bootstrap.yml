server:
  port: 9111
spring:
  application:
    name: wms-server
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 172.18.35.23:8181
        namespace: wms-server-dev
        context-path: "/nacos"
        group: swms
        file-extension: yml
      discovery:
        namespace: wms-server-dev
        server-addr: 172.18.35.23:8181
        username: nacos
        password: nacos
        group: swms
  config:
    import:
      - nacos:wms-server.yml
      - nacos:wms-server-redisson.yml
