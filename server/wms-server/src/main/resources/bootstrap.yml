spring:
  application:
    name: wms
  profiles:
    active: test
  cloud:
    nacos:
      config:
        server-addr: ${nacos.hairoutech.com:192.168.83.128}:${nacos.port:8848}
        prefix: ${spring.application.name}
        file-extension: yml
        namespace: ${spring.profiles.active}
        group: swms
        username: nacos
        password: nacos

server:
  port: 9030

dubbo:
  application:
    id: search
    name: search
  consumer:
    check: false
  scan:
    base-packages: com.swms
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://192.168.83.128:8848
    timeout: 10000
  cloud:
    subscribed-services: tenant
